# Copy to .env.local for local development. On Vercel, set in Project Settings → Environment Variables.

# Base URL of the API (the Render URL in production)
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Optional: same Google OAuth client ID as the API — shows the Google sign-in button
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
